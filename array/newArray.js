//JavaScript has a built-in array constructor new Array().

// But you can safely use [] instead.

// These two different statements both create a new empty array named po
//const  points =new Array();
//const points =[];

const points = new Array(40,80,120,150,200);
//const points =[40,100,120,120,300];
document.write(points[4]);